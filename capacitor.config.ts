import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tonu.reactuniversal',
  appName: 'react-universal-app',
  webDir: 'build'
};

export default config;
